.DS_Store
.env
.Rhistory

# Machine-local agent context — never commit
CLAUDE.local.md
AGENTS.local.md

# Terraform — state and vars contain secrets; never commit
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/
