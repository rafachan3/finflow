.DS_Store
.env
.Rhistory

# Machine-local agent context — never commit
CLAUDE.local.md
AGENTS.local.md

# Terraform — state and vars contain secrets; never commit
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/

# Personal seed overlay and imported personal data — never commit
supabase/seed.personal.sql
scripts/import/data/

# Supabase CLI local artifacts
supabase/.temp/
