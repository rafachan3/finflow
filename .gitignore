.DS_Store
.env
.Rhistory

# Machine-local / owner-specific context — never commit
# (CLAUDE.local.md, AGENTS.local.md, docs/BUCKET_RULES.local.md, ...)
*.local.md

# Agent working notes / brainstorming specs — local only, never commit
docs/superpowers/

# Terraform — state and vars contain secrets; never commit
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/

# Personal seed overlay and one-off import tooling — owner-specific
# (contains personal taxonomy values); never commit
supabase/seed.personal.sql
scripts/import/

# Supabase CLI local artifacts
supabase/.temp/
